# ============================================
# ClassVision 课堂注意力智能分析系统
# Python 3.11+ 依赖清单
# 安装方式: pip install -r requirements.txt
# ============================================

# --- Web 框架 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# --- CV 视觉算法 ---
ultralytics>=8.3.0           # YOLOv8 人脸检测 + 内置 ByteTrack 多目标跟踪
mediapipe>=0.10.9            # Google MediaPipe FaceMesh 468 关键点
opencv-python>=4.8.0         # 图像处理
numpy>=1.24.0                # 数值计算（版本由 pip 自动解决兼容性）
insightface>=0.7.3           # 人脸识别特征提取（ArcFace ONNX）
onnxruntime>=1.16.0          # ONNX 推理引擎（轻量，无需 TensorFlow）

# --- 大模型 ---
ollama==0.4.7                # Ollama Python SDK，调用本地大模型

# --- 数据库 ---
sqlalchemy==2.0.36           # ORM

# --- 配置与工具 ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0  # JWT 鉴权
bcrypt>=4.0.0                     # 密码哈希

# --- RAG 模块 ---
sentence-transformers>=2.2.0   # 文本嵌入模型
faiss-cpu>=1.7.4               # 向量检索索引（Windows用cpu版）
pdfplumber>=0.7.0              # PDF解析
pymupdf>=1.23.0                # PDF解析（实际使用，比pdfplumber快）
python-docx>=1.1.0             # Word文档解析
python-pptx>=0.6.23            # PPT课件解析
jieba>=0.42.1                  # 中文分词（BM25稀疏检索必需）
rank-bm25>=0.2.2               # BM25稀疏检索算法
tiktoken>=0.7.0                # token 计数分块（消除中英文字符长度失真）

# --- 批量导入 ---
openpyxl>=3.1.0               # Excel读写（批量导入花名册）

# --- 开发工具 ---
requests>=2.31.0              # HTTP 客户端（测试脚本使用）
